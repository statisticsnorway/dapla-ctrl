query ConsumesSharedData($team: Slug!) {
	team(slug: $team) {
		id
		slug
		sharedBuckets(first: 1) {
			pageInfo {
				totalCount
			}
		}
		sharedBucketsAccess(first: 25) @paginate(mode: SinglePage) {
			pageInfo {
				hasNextPage
				hasPreviousPage
				pageEnd
				pageStart
				startCursor
				endCursor
				totalCount
			}
			nodes {
				id
				name
				shortName
				kind
				env
				uniqueUsers {
					pageInfo {
						totalCount
					}
				}
				teams {
					pageInfo {
						totalCount
					}
				}
			}
		}
	}
}
