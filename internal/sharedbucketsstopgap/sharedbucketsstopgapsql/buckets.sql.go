// Code generated by sqlc. DO NOT EDIT.
// source: buckets.sql

package sharedbucketsstopgapsql

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
	"github.com/statisticsnorway/dapla-api/internal/slug"
)

const getByNames = `-- name: GetByNames :many
SELECT
	name, team_slug, short_name, kind, env
FROM
	shared_buckets_stopgap
WHERE
	name = ANY ($1::TEXT[])
ORDER BY
	name
`

func (q *Queries) GetByNames(ctx context.Context, names []string) ([]*SharedBucketsStopgap, error) {
	rows, err := q.db.Query(ctx, getByNames, names)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*SharedBucketsStopgap{}
	for rows.Next() {
		var i SharedBucketsStopgap
		if err := rows.Scan(
			&i.Name,
			&i.TeamSlug,
			&i.ShortName,
			&i.Kind,
			&i.Env,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const list = `-- name: List :many
SELECT
	shared_buckets_stopgap.name, shared_buckets_stopgap.team_slug, shared_buckets_stopgap.short_name, shared_buckets_stopgap.kind, shared_buckets_stopgap.env,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_stopgap
ORDER BY
	CASE
		WHEN $1::TEXT = 'name:asc' THEN name
	END ASC,
	CASE
		WHEN $1::TEXT = 'name:desc' THEN name
	END DESC,
	CASE
		WHEN $1::TEXT = 'kind:asc' THEN kind
	END ASC,
	CASE
		WHEN $1::TEXT = 'kind:desc' THEN kind
	END DESC,
	CASE
		WHEN $1::TEXT = 'short_name:asc' THEN short_name
	END ASC,
	CASE
		WHEN $1::TEXT = 'short_name:desc' THEN short_name
	END DESC,
	CASE
		WHEN $1::TEXT = 'env:asc' THEN env
	END ASC,
	CASE
		WHEN $1::TEXT = 'env:desc' THEN env
	END DESC,
	CASE
		WHEN $1::TEXT = 'team:asc' THEN team_slug
	END ASC,
	CASE
		WHEN $1::TEXT = 'team:desc' THEN team_slug
	END DESC,
	short_name ASC
LIMIT
	$3
OFFSET
	$2
`

type ListParams struct {
	OrderBy string
	Offset  int32
	Limit   int32
}

type ListRow struct {
	SharedBucketsStopgap SharedBucketsStopgap
	TotalCount           int64
}

func (q *Queries) List(ctx context.Context, arg ListParams) ([]*ListRow, error) {
	rows, err := q.db.Query(ctx, list, arg.OrderBy, arg.Offset, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListRow{}
	for rows.Next() {
		var i ListRow
		if err := rows.Scan(
			&i.SharedBucketsStopgap.Name,
			&i.SharedBucketsStopgap.TeamSlug,
			&i.SharedBucketsStopgap.ShortName,
			&i.SharedBucketsStopgap.Kind,
			&i.SharedBucketsStopgap.Env,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAccessToForTeam = `-- name: ListAccessToForTeam :many
SELECT
	shared_buckets_stopgap.name, shared_buckets_stopgap.team_slug, shared_buckets_stopgap.short_name, shared_buckets_stopgap.kind, shared_buckets_stopgap.env,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_stopgap
	JOIN shared_buckets_access_stopgap ON shared_buckets_access_stopgap.bucket_name = shared_buckets_stopgap.name
	JOIN groups ON shared_buckets_access_stopgap.group_name = groups.name
WHERE
	groups.team_slug = $1
GROUP BY
	shared_buckets_stopgap.name,
	groups.team_slug
ORDER BY
	CASE
		WHEN $2::TEXT = 'name:asc' THEN shared_buckets_stopgap.name
	END ASC,
	CASE
		WHEN $2::TEXT = 'name:desc' THEN shared_buckets_stopgap.name
	END DESC,
	CASE
		WHEN $2::TEXT = 'kind:asc' THEN kind
	END ASC,
	CASE
		WHEN $2::TEXT = 'kind:desc' THEN kind
	END DESC,
	CASE
		WHEN $2::TEXT = 'short_name:asc' THEN short_name
	END ASC,
	CASE
		WHEN $2::TEXT = 'short_name:desc' THEN short_name
	END DESC,
	CASE
		WHEN $2::TEXT = 'env:asc' THEN env
	END ASC,
	CASE
		WHEN $2::TEXT = 'env:desc' THEN env
	END DESC,
	CASE
		WHEN $2::TEXT = 'team:asc' THEN shared_buckets_stopgap.team_slug
	END ASC,
	CASE
		WHEN $2::TEXT = 'team:desc' THEN shared_buckets_stopgap.team_slug
	END DESC,
	short_name ASC
LIMIT
	$4
OFFSET
	$3
`

type ListAccessToForTeamParams struct {
	TeamSlug slug.Slug
	OrderBy  string
	Offset   int32
	Limit    int32
}

type ListAccessToForTeamRow struct {
	SharedBucketsStopgap SharedBucketsStopgap
	TotalCount           int64
}

func (q *Queries) ListAccessToForTeam(ctx context.Context, arg ListAccessToForTeamParams) ([]*ListAccessToForTeamRow, error) {
	rows, err := q.db.Query(ctx, listAccessToForTeam,
		arg.TeamSlug,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListAccessToForTeamRow{}
	for rows.Next() {
		var i ListAccessToForTeamRow
		if err := rows.Scan(
			&i.SharedBucketsStopgap.Name,
			&i.SharedBucketsStopgap.TeamSlug,
			&i.SharedBucketsStopgap.ShortName,
			&i.SharedBucketsStopgap.Kind,
			&i.SharedBucketsStopgap.Env,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAllForSearch = `-- name: ListAllForSearch :many
SELECT
	name,
	team_slug
FROM
	shared_buckets_stopgap
ORDER BY
	name ASC
`

type ListAllForSearchRow struct {
	Name     string
	TeamSlug slug.Slug
}

func (q *Queries) ListAllForSearch(ctx context.Context) ([]*ListAllForSearchRow, error) {
	rows, err := q.db.Query(ctx, listAllForSearch)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListAllForSearchRow{}
	for rows.Next() {
		var i ListAllForSearchRow
		if err := rows.Scan(&i.Name, &i.TeamSlug); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listForTeam = `-- name: ListForTeam :many
SELECT
	shared_buckets_stopgap.name, shared_buckets_stopgap.team_slug, shared_buckets_stopgap.short_name, shared_buckets_stopgap.kind, shared_buckets_stopgap.env,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_stopgap
WHERE
	team_slug = $1
ORDER BY
	CASE
		WHEN $2::TEXT = 'name:asc' THEN name
	END ASC,
	CASE
		WHEN $2::TEXT = 'name:desc' THEN name
	END DESC,
	CASE
		WHEN $2::TEXT = 'kind:asc' THEN kind
	END ASC,
	CASE
		WHEN $2::TEXT = 'kind:desc' THEN kind
	END DESC,
	CASE
		WHEN $2::TEXT = 'short_name:asc' THEN short_name
	END ASC,
	CASE
		WHEN $2::TEXT = 'short_name:desc' THEN short_name
	END DESC,
	CASE
		WHEN $2::TEXT = 'env:asc' THEN env
	END ASC,
	CASE
		WHEN $2::TEXT = 'env:desc' THEN env
	END DESC,
	CASE
		WHEN $2::TEXT = 'team:asc' THEN team_slug
	END ASC,
	CASE
		WHEN $2::TEXT = 'team:desc' THEN team_slug
	END DESC,
	short_name ASC
LIMIT
	$4
OFFSET
	$3
`

type ListForTeamParams struct {
	TeamSlug slug.Slug
	OrderBy  string
	Offset   int32
	Limit    int32
}

type ListForTeamRow struct {
	SharedBucketsStopgap SharedBucketsStopgap
	TotalCount           int64
}

func (q *Queries) ListForTeam(ctx context.Context, arg ListForTeamParams) ([]*ListForTeamRow, error) {
	rows, err := q.db.Query(ctx, listForTeam,
		arg.TeamSlug,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListForTeamRow{}
	for rows.Next() {
		var i ListForTeamRow
		if err := rows.Scan(
			&i.SharedBucketsStopgap.Name,
			&i.SharedBucketsStopgap.TeamSlug,
			&i.SharedBucketsStopgap.ShortName,
			&i.SharedBucketsStopgap.Kind,
			&i.SharedBucketsStopgap.Env,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listForUser = `-- name: ListForUser :many
SELECT
	shared_buckets_stopgap.name, shared_buckets_stopgap.team_slug, shared_buckets_stopgap.short_name, shared_buckets_stopgap.kind, shared_buckets_stopgap.env,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_stopgap
	JOIN shared_buckets_access_stopgap ON shared_buckets_access_stopgap.bucket_name = shared_buckets_stopgap.name
	JOIN group_members ON shared_buckets_access_stopgap.group_name = group_members.group_name
WHERE
	group_members.user_id = $1
GROUP BY
	name
ORDER BY
	CASE
		WHEN $2::TEXT = 'name:asc' THEN name
	END ASC,
	CASE
		WHEN $2::TEXT = 'name:desc' THEN name
	END DESC,
	CASE
		WHEN $2::TEXT = 'kind:asc' THEN kind
	END ASC,
	CASE
		WHEN $2::TEXT = 'kind:desc' THEN kind
	END DESC,
	CASE
		WHEN $2::TEXT = 'short_name:asc' THEN short_name
	END ASC,
	CASE
		WHEN $2::TEXT = 'short_name:desc' THEN short_name
	END DESC,
	CASE
		WHEN $2::TEXT = 'env:asc' THEN env
	END ASC,
	CASE
		WHEN $2::TEXT = 'env:desc' THEN env
	END DESC,
	CASE
		WHEN $2::TEXT = 'team:asc' THEN team_slug
	END ASC,
	CASE
		WHEN $2::TEXT = 'team:desc' THEN team_slug
	END DESC,
	short_name ASC
LIMIT
	$4
OFFSET
	$3
`

type ListForUserParams struct {
	UserID  uuid.UUID
	OrderBy string
	Offset  int32
	Limit   int32
}

type ListForUserRow struct {
	SharedBucketsStopgap SharedBucketsStopgap
	TotalCount           int64
}

func (q *Queries) ListForUser(ctx context.Context, arg ListForUserParams) ([]*ListForUserRow, error) {
	rows, err := q.db.Query(ctx, listForUser,
		arg.UserID,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListForUserRow{}
	for rows.Next() {
		var i ListForUserRow
		if err := rows.Scan(
			&i.SharedBucketsStopgap.Name,
			&i.SharedBucketsStopgap.TeamSlug,
			&i.SharedBucketsStopgap.ShortName,
			&i.SharedBucketsStopgap.Kind,
			&i.SharedBucketsStopgap.Env,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGroupsForBucket = `-- name: ListGroupsForBucket :many
SELECT
	groups.name, groups.team_slug, groups.category, groups.suffix, groups.external_id,
	COUNT(*) OVER () AS total_count
FROM
	groups
	JOIN shared_buckets_access_stopgap ON groups.name = shared_buckets_access_stopgap.group_name
WHERE
	shared_buckets_access_stopgap.bucket_name = $1
ORDER BY
	CASE
		WHEN $2::TEXT = 'name:asc' THEN groups.name
	END ASC,
	CASE
		WHEN $2::TEXT = 'name:desc' THEN groups.name
	END DESC,
	CASE
		WHEN $2::TEXT = 'team:asc' THEN team_slug
	END ASC,
	CASE
		WHEN $2::TEXT = 'team:desc' THEN team_slug
	END DESC,
	CASE
		WHEN $2::TEXT = 'category:asc' THEN category
	END ASC,
	CASE
		WHEN $2::TEXT = 'category:desc' THEN category
	END DESC,
	name ASC
LIMIT
	$4
OFFSET
	$3
`

type ListGroupsForBucketParams struct {
	Name    string
	OrderBy string
	Offset  int32
	Limit   int32
}

type ListGroupsForBucketRow struct {
	Group      Group
	TotalCount int64
}

func (q *Queries) ListGroupsForBucket(ctx context.Context, arg ListGroupsForBucketParams) ([]*ListGroupsForBucketRow, error) {
	rows, err := q.db.Query(ctx, listGroupsForBucket,
		arg.Name,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListGroupsForBucketRow{}
	for rows.Next() {
		var i ListGroupsForBucketRow
		if err := rows.Scan(
			&i.Group.Name,
			&i.Group.TeamSlug,
			&i.Group.Category,
			&i.Group.Suffix,
			&i.Group.ExternalID,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamsForBucket = `-- name: ListTeamsForBucket :many
SELECT
	teams.slug, teams.display_name, teams.last_successful_sync, teams.delete_key_confirmed_at, teams.section_code, teams.is_managed,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_access_stopgap
	JOIN groups ON groups.name = shared_buckets_access_stopgap.group_name
	JOIN teams ON groups.team_slug = teams.slug
WHERE
	shared_buckets_access_stopgap.bucket_name = $1
GROUP BY
	teams.slug
ORDER BY
	CASE
		WHEN $2::TEXT = 'slug:asc' THEN teams.slug
	END ASC,
	CASE
		WHEN $2::TEXT = 'slug:desc' THEN teams.slug
	END DESC,
	CASE
		WHEN $2::TEXT = 'section_code:asc' THEN teams.section_code
	END ASC,
	CASE
		WHEN $2::TEXT = 'section_code:desc' THEN teams.section_code
	END DESC,
	teams.slug ASC
LIMIT
	$4
OFFSET
	$3
`

type ListTeamsForBucketParams struct {
	Name    string
	OrderBy string
	Offset  int32
	Limit   int32
}

type ListTeamsForBucketRow struct {
	Slug                 slug.Slug
	DisplayName          string
	LastSuccessfulSync   pgtype.Timestamp
	DeleteKeyConfirmedAt pgtype.Timestamptz
	SectionCode          string
	IsManaged            bool
	TotalCount           int64
}

func (q *Queries) ListTeamsForBucket(ctx context.Context, arg ListTeamsForBucketParams) ([]*ListTeamsForBucketRow, error) {
	rows, err := q.db.Query(ctx, listTeamsForBucket,
		arg.Name,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListTeamsForBucketRow{}
	for rows.Next() {
		var i ListTeamsForBucketRow
		if err := rows.Scan(
			&i.Slug,
			&i.DisplayName,
			&i.LastSuccessfulSync,
			&i.DeleteKeyConfirmedAt,
			&i.SectionCode,
			&i.IsManaged,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUniqueUsersForBucket = `-- name: ListUniqueUsersForBucket :many
SELECT
	users.id, users.email, users.name, users.external_id, users.admin, users.section_code, users.job_title, users.employment_type,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_access_stopgap
	JOIN group_members ON group_members.group_name = shared_buckets_access_stopgap.group_name
	JOIN users ON group_members.user_id = users.id
WHERE
	shared_buckets_access_stopgap.bucket_name = $1
GROUP BY
	users.id
ORDER BY
	CASE
		WHEN $2::TEXT = 'name:asc' THEN LOWER(users.name)
	END ASC,
	CASE
		WHEN $2::TEXT = 'name:desc' THEN LOWER(users.name)
	END DESC,
	CASE
		WHEN $2::TEXT = 'email:asc' THEN users.email
	END ASC,
	CASE
		WHEN $2::TEXT = 'email:desc' THEN users.email
	END DESC,
	CASE
		WHEN $2::TEXT = 'section_code:asc' THEN users.section_code
	END ASC,
	CASE
		WHEN $2::TEXT = 'section_code:desc' THEN users.section_code
	END DESC,
	users.name,
	users.email ASC
LIMIT
	$4
OFFSET
	$3
`

type ListUniqueUsersForBucketParams struct {
	Name    string
	OrderBy string
	Offset  int32
	Limit   int32
}

type ListUniqueUsersForBucketRow struct {
	ID             uuid.UUID
	Email          string
	Name           string
	ExternalID     string
	Admin          bool
	SectionCode    *string
	JobTitle       *string
	EmploymentType string
	TotalCount     int64
}

func (q *Queries) ListUniqueUsersForBucket(ctx context.Context, arg ListUniqueUsersForBucketParams) ([]*ListUniqueUsersForBucketRow, error) {
	rows, err := q.db.Query(ctx, listUniqueUsersForBucket,
		arg.Name,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListUniqueUsersForBucketRow{}
	for rows.Next() {
		var i ListUniqueUsersForBucketRow
		if err := rows.Scan(
			&i.ID,
			&i.Email,
			&i.Name,
			&i.ExternalID,
			&i.Admin,
			&i.SectionCode,
			&i.JobTitle,
			&i.EmploymentType,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUsersForBucket = `-- name: ListUsersForBucket :many
SELECT
	groups.team_slug,
	users.id AS user_id,
	COUNT(*) OVER () AS total_count
FROM
	shared_buckets_access_stopgap
	JOIN group_members ON group_members.group_name = shared_buckets_access_stopgap.group_name
	JOIN users ON group_members.user_id = users.id
	JOIN groups ON groups.name = group_members.group_name
WHERE
	shared_buckets_access_stopgap.bucket_name = $1
GROUP BY
	groups.team_slug,
	users.id
ORDER BY
	CASE
		WHEN $2::TEXT = 'name:asc' THEN LOWER(users.name)
	END ASC,
	CASE
		WHEN $2::TEXT = 'name:desc' THEN LOWER(users.name)
	END DESC,
	CASE
		WHEN $2::TEXT = 'email:asc' THEN users.email
	END ASC,
	CASE
		WHEN $2::TEXT = 'email:desc' THEN users.email
	END DESC,
	CASE
		WHEN $2::TEXT = 'section_code:asc' THEN users.section_code
	END ASC,
	CASE
		WHEN $2::TEXT = 'section_code:desc' THEN users.section_code
	END DESC,
	users.name,
	users.email ASC
LIMIT
	$4
OFFSET
	$3
`

type ListUsersForBucketParams struct {
	Name    string
	OrderBy string
	Offset  int32
	Limit   int32
}

type ListUsersForBucketRow struct {
	TeamSlug   slug.Slug
	UserID     uuid.UUID
	TotalCount int64
}

func (q *Queries) ListUsersForBucket(ctx context.Context, arg ListUsersForBucketParams) ([]*ListUsersForBucketRow, error) {
	rows, err := q.db.Query(ctx, listUsersForBucket,
		arg.Name,
		arg.OrderBy,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListUsersForBucketRow{}
	for rows.Next() {
		var i ListUsersForBucketRow
		if err := rows.Scan(&i.TeamSlug, &i.UserID, &i.TotalCount); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
